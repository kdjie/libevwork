2017.3.6  支持可将事件循环放到线程中运行，通过将环境变量线程局部存储实现。
2018.7.13 #define LOG(l,f,...) CEnv::getThreadEnv()->getLogger()->log(l, f, ##__VA_ARGS__)
2018.8.7 加入dsmfc类yy序列化/反序列化支持。
2019.4.9 加入日志级别可限制，避免日志过多造成rsyslog丢失。加入可输出到终端。